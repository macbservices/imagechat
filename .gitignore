# Build
dist/

# Deps
node_modules/

# Env / secrets — nunca commitar
.env
.env.local
.env.*.local
functions/.env
functions/.env.*
!functions/.env.example

# Firebase
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*
*.log

# Chave de service account — NUNCA commitar (vai só na env var da Vercel)
*firebase-adminsdk*.json
*serviceAccount*.json
service-account*.json

# Editor / OS
.DS_Store
.vscode/

# Ferramentas de agente (bundle local de skills) — não fazem parte do produto
.agents/
.claude/
skills-lock.json
